/** @jsx jsx */
import { jsx, Flex } from "theme-ui"

const Header = () => {
  return (
    <Flex
      as="header"
      sx={{
        justifyContent: "space-between",
      }}
    >
      header
    </Flex>
  )
}

export default Header
